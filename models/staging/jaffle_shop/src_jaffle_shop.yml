version: 2

sources:
  - name: jaffle_shop
    database: dbt_raw
    schema: jaffle_shop
    tables:
      - name: customers
        columns:
          - name: id
            description: primary key for customers source table
            tests:
              - unique
              - not_null
              
      - name: orders
        columns:
          - name: id
            description: primary key for source table orders
            tests:
              - unique
              - not_null                  
        loaded_at_field: _etl_loaded_at
        freshness:
          warn_after: {count: 48, period: hour}
          error_after: {count: 500, period: hour}