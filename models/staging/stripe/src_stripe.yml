
version: 2

sources:
  - name: stripe
    database: dbt_raw
    schema: stripe
    tables:
      - name: payment
        description: source table for payments
        columns:
          - name: id
            description: primary key for payments source table
            tests:
              - unique
              - not_null
        loaded_at_field: _batched_at
        freshness:
          warn_after: {count: 48, period: hour}
          error_after: {count: 500, period: hour}